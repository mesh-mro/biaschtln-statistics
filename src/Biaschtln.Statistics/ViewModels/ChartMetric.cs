namespace Biaschtln.Statistics.ViewModels;

/// <summary>Wählt, ob eine Auswertung den Umsatz oder die Anzahl Positionen zeigt.</summary>
public enum ChartMetric
{
    /// <summary>Umsatz (Summe Preis).</summary>
    Revenue,

    /// <summary>Anzahl Positionen.</summary>
    Count,
}
